import cloudyIcon from "./cloudyIcon.png";

const CloudyIcon = () => {
  return (
    <img
      src={cloudyIcon}
      alt="Cloudy Icon"
      style={{ height: "64px", width: "64px" }}
    />
  );
};

export default CloudyIcon;
